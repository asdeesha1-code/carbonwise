# Pasted Content Applied to CarbonWise

The attached content describes the CarbonWise experience that is already present in the project. Its first-screen requirement is represented by the Today route, which shows the user location, local time, current weather context, a horizontally scrollable hourly forecast, and the expected climate signal for the day.

The listed expansion is represented by local prototype surfaces for climate-aware feedback, history, weather, source-linked climate education, learning, account/settings, climate-fund presentation, payment-history placeholder, optional daily reminders, exit confirmation where supported, and widget-style quick access.

The final three items in the pasted content are production integrations rather than safe local prototype behavior:

| Requested production step | Current CarbonWise state | Required next implementation |
|---|---|---|
| Approved live weather API and news feed | Local forecast and official NASA/UNEP links | Select providers, configure credentials, add caching, error states, and freshness labels |
| Native home-screen widgets and notification settings | In-app widget preview and local reminder helper | Add a development build and platform-specific widget modules, then expose permission/settings controls |
| Compliant payment provider | Fund presentation and payment-history prototype surface | Select a compliant provider, add server-side payment verification, receipts, refund handling, and legal copy |

No contribution is processed, no live weather is claimed, and no demo weather value is presented as a real measurement. The prototype falls back locally until these production integrations are deliberately configured.
